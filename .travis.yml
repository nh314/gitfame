language: node_js
node_js:
  - "lts/*"
cache:
  directories:
    - node_modules

script:
  - yarn test
  - yarn build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: "build"
  on:
    branch: develop
